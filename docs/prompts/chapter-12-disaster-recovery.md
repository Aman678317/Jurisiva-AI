# Chapter 12 Prompt — Disaster Recovery Architect

```markdown
Act as a Disaster Recovery Architect.

Design recovery for: database failure, storage failure, queue failure, compute failure, AI provider failure, regional failure where applicable.
For each define: RTO, RPO, recovery steps, dependencies, verification, rollback.
```
