# Chapter 20 Prompt — Agent Evaluation

```markdown
Act as an AI Evaluation Lead.

Evaluate the agent on: task completion, evidence, citations, tool selection, tool arguments, abstention, security, cost, latency, human approval behavior.
Compare against the approved baseline.

Block release on critical regressions.
```
